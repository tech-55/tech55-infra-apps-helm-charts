
{{- $appName := include "app.toLower" .Values.appName -}}
{{- $envHPA := include "app.envValue" (dict "Values" .Values "Key" "autoscaling" "Env" .Values.awsAccountAlias) | fromYaml -}}

apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ $appName }}-hpa
  namespace: {{ .Values.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ $appName }}-deployment
  minReplicas: {{ $envHPA.minReplicas }}
  maxReplicas: {{ $envHPA.maxReplicas }}
  {{- if or $envHPA.targetCPUAverageValue $envHPA.targetMemoryAverageValue }}
  metrics:
    {{- if $envHPA.targetCPUAverageValue }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: AverageValue
          averageValue: {{ $envHPA.targetCPUAverageValue }}
    {{- end }}
    {{- if $envHPA.targetMemoryAverageValue }}
    - type: Resource
      resource:
        name: memory
        target:
          type: AverageValue
          averageValue: {{ $envHPA.targetMemoryAverageValue }}
    {{- end }}
  {{- end }}
