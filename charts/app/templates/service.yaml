{{- $envName := include "app.envName" . -}}
{{- $appName := include "app.appName" . -}}

apiVersion: v1
kind: Service
metadata:
  name: {{ $appName }}-{{$envName}}-service
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: {{ $appName }}
    environment: {{ $envName }}
  ports:
    - port: 80
      targetPort: {{.Values.image.port}}
