{{- $envName := include "app.envName" . -}}
{{- $appName := include "app.appName" . -}}
{{- $envServiceAccount := include "app.envValue" (dict "Values" .Values "Key" "serviceAccount" "Env" .Values.environmentName) | fromYaml -}}

{{- if $envServiceAccount.roleArn }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{$appName}}-{{$envName}}-sa
  namespace: {{ .Values.namespace }}
  annotations:
    eks.amazonaws.com/role-arn: "{{ $envServiceAccount.roleArn }}"
{{- end }}